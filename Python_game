# Space-War game
# Space War game using Python
# Modules used = Pygame, Random, Math
# This game is based upon legendry space war game
# In this a player controls the spaceship and tries to kill the aliens before the reach him
# and tries to create the highest score.
# Lets Start




import math      # maths for aliens and score generation
import random    # for random appeaearence of aliens

import pygame    # this is the most important module
from pygame import mixer

# Intialize the pygame
pygame.init()

# create the screen
screen = pygame.display.set_mode((800, 600))    # screen and screen size

# Background
background = pygame.image.load('background.png')   # background image

# Sound                               # game sound
mixer.music.load("background.wav")
mixer.music.play(-1)


# Caption and Icon
pygame.display.set_caption("Space-War ")
icon = pygame.image.load('ufo.png')     # game icon
pygame.display.set_icon(icon)

# Player
playerImg = pygame.image.load('spaceship.png')   # player or spaceship image
playerX = 370
playerY = 480
playerX_change = 0

# Enemy
enemyImg = []
enemyX = []
enemyY = []
enemyX_change = []
enemyY_change = []
num_of_enemies = 6     # number of enemies on screen





